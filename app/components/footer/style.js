'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  footer: {
    height: 40,
    borderColor: '#c3c3c3',
    borderTopWidth: 1,
    alignItems: 'center'
  },
  image: {
    height: 50,
    width: 50,
    top: -15,
  }
});
