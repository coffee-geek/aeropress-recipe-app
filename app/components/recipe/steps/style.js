'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  title: {
    fontSize: 20,
    marginTop: 10,
    textAlign: 'center'
  }
});
